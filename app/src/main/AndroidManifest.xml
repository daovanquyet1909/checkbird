<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.birdclassifier">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Checkbird">

        <!-- Splash Activity -->
        <activity
            android:name="com.example.birdclassifier.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name="com.example.birdclassifier.MainActivity"
            android:exported="true">
        </activity>

        <!-- Other activities -->
        <activity
            android:name="com.example.birdclassifier.BirdListActivity"
            android:exported="false">
        </activity>
        <activity
            android:name="com.example.birdclassifier.SearchByImageActivity"
            android:exported="false">
        </activity>
        <activity
            android:name="com.example.birdclassifier.BirdDetailActivity"
            android:exported="false">
        </activity>

    </application>
</manifest>
